export enum TRIGGERS_TEXT {
    ROI = "ROI (%)",
    CHANGE = "Change (%)",
    PL = "P&L"
}

export enum TRIGGERS {
    ROI = "ROI",
    CHANGE = "CHANGE",
    PL = "PL"
}

export enum TRADE_POSITION {
    LONG = "Long",
    SHORT = "Short"
}

export enum ORDER_TYPE {
    PROFIT = "profit",
    STOP = "stop"
}