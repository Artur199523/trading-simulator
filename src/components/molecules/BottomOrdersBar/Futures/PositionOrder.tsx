import React from "react"

import {positionHeader} from "utils";

import {FlexibleTable} from "components";

const PositionOrder = () => {
    return (
        <FlexibleTable header={positionHeader} body={[]}/>
    )
}

export default PositionOrder